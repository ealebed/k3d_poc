apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-http-server-web
spec:
  template:
    spec:
      containers:
      - name: web
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 256Mi
